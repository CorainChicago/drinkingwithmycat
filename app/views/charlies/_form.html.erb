<%= form_for(@charlie) do |f| %>
  <% if @charlie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@charlie.errors.count, "error") %> prohibited this charly from being saved:</h2>

      <ul>
      <% @charlie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="new_charlie_form col-md-12">
  <h2>Add a New Charlie Cat</h2>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
</div>  
<% end %>
